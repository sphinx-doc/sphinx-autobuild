language: python
sudo: false

python:
  - 3.5
  - 3.6
  - 3.7
  - 3.8

install:
  - pip install nox
  - nox -s test-${TRAVIS_PYTHON_VERSION} --install-only

script:
  - nox -s test-${TRAVIS_PYTHON_VERSION}
